class PDFThing {
  /**
   *
   * @param {*} File
   * @param {*} Template
   */
  constructor(File, Template) {
    this.File = File;
    this.Template = Template;
  }

  calcArea() {}

  IsValid() {}
}
